import { animated } from 'react-spring';
import styled from 'styled-components';

export const Container = styled(animated.h2)`
  position: relative;
  margin: 0 0 2rem 0;
  font-size: 4rem;
  text-transform: uppercase;
  overflow: hidden;
`;
