import React from 'react';
import PropTypes from 'prop-types';
import { Container } from './Cursor.style';

function Cursor(props) {
  return <Container {...props} />;
}

Cursor.propTypes = {};

export default Cursor;
